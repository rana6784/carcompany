import 'package:flutter/material.dart';

class BookingPages extends StatelessWidget {
  const BookingPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Booking pages")));
  }
}
